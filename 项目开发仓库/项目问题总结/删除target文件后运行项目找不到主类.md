我在写分页查询时，一直发送请求返回404，检查了代码确认无误后，我想到了把项目进行重新编译
但是我在删除target文件后，再次运行项目发现抱错找不到主类。
这时需要在maven中clean一下，然后再次compile，在菜单栏上面找到build -> 点击重新编译后
重新运行就解决了。

当我在运行模糊查询时，发现要么单独name可以查询，要么phone单独可以查询，但是两个组合在一起就不行了? 这是什么问题，我看了我的xml中的sql，原来时动态sql拼接时，第二句if语句忘了加and 条件

![[Pasted image 20230603194630.png]]

我修改了语句，重启项目还是无法查询到数据，这时我又打开了target文件，发现target文件中的语句还是没变，我将target文件中的xml文件删除，然后像之前一样重新进行了编译，重新运行项目，
这一次，我的target文件中有了我修改后的sql语句。

![[Pasted image 20230603194736.png]]

这一次终于查询出来了
![[Pasted image 20230603195531.png]]
